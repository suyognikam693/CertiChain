{
  "name": "credential-verifier-blockchain",
  "version": "1.0.0",
  "description": "Hardhat project for CredentialVerifier smart contract",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.3",
    "hardhat": "^3.3.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "type": "module"
}
